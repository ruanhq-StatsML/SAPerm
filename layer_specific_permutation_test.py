#different layers for fine-tuning:


#np.searchsorted
#inject a couple of random words that is not related to the change of the marginal distributions.

def layer_specific_distance(df:pd.DataFrame,
    text_col: str, base_model_id, base_model_id,
    )